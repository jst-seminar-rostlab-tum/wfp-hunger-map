export const siteConfig = {
  name: 'HungerMap LIVE',
  description: 'HungerMap LIVE by mVAM',
  navItems: [
    {
      label: 'Home',
      href: '/',
    },
  ],
  navMenuItems: [],
  links: {},
};

export type SiteConfig = typeof siteConfig;
